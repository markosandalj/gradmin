export const userMenuActions = [
    {
      items: [
        // { content: <a href={`${window.location.origin}/admin`}>Django admin</a> },
        // { content: <a href={`${window.location.origin}/cms`}>Wagtail admin</a> },
        // { content: <a href={`${window.location.origin}/index`}>Index admin</a> }
        { content: 'Django admin' },
        { content: 'Wagtail admin' },
        { content: 'Index admin' }
    ],
    },
  ];